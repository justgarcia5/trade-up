<% if user_signed_in? %>
  <%= react_component "pages/Landing", current_user: {
      id: current_user.id,
      email: current_user.email,
      name: current_user.name,
    } %>
<% else %>
  <%= react_component "pages/Landing" %>
<% end %>
